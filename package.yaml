name: voxf
version: 0.0.1
synopsis: game
description: game
maintainer: ivxvm <ivxvm@protonmail.com>
github: ivxvm/voxf
category: Gamedev

# ghc-options: -Wall

executable:
  main: Main.hs
  source-dirs: app
  dependencies:
    - base >= 4.16
    - containers >= 0.6
  default-extensions:
    - OverloadedRecordDot
    - DuplicateRecordFields
